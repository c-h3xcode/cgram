// AUTOGENERATED BY CGRAM_GENERATOR
// API VERSION: {{ api.version }}
// API RELEASE DATE: {{ api.release_date }}
// API CHANGELOG: {{ api.changelog }}

#ifndef CGRAM_METHODS_{{ method.name|upper }}_H
#define CGRAM_METHODS_{{ method.name|upper }}_H

#include "../types.h"

result({{method.returns.name}}) cgram_methods_{{method.name}}(
    {%- for field in method.fields %}

    {% if field.type.cgram_type%}
    type({{field.type.name}}) {{field.name}}{% if not loop.last %},{% endif %}
    {% else %}
    {{field.type.name}} {{field.name}}{% if not loop.last %},{% endif %}
    {% endif %}
    {% endfor %}

);

#endif // CGRAM_METHODS_{{ method.name|upper }}_H
